import React from 'react'
import {render} from '@testing-library/react'

// import Events from '../components/Events'

// TODO: Add Events component tests, gets a `Target container is not a DOM element` at the moment.
test('render Events', () => {
  expect(true).toBe(true)
  // const {
  //   getByText
  // } = render( < Events / > )
})
